<h3><%= @patient.name %> appointments</h3>

<ul>
<% @patient.appointments.each do |appointment| %>
<li>
<%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M') %> / Doctor: <%= link_to appointment.doctor.name, doctor_path(appointment.doctor.id) %>
</li>
<% end %>
</ul>
