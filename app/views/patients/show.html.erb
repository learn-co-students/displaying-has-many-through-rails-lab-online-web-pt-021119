<h1><%= @patient.name %></h1>

<h3>Appointments:</h3>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li>
      <p>
        Dr. <%= link_to appointment.doctor.name, doctor_path(appointment.doctor.id) %>, <%= appointment.time %>
      </p>
    </li>
  <% end %>
</ul>
