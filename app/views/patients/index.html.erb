<h1>Patients</h1>

<ul>
  <% @patients.each do |patient| %>
  <li>
    Name: <%= link_to patient.name, patient_path(patient.id) %> Number of appointments: <%= patient.appointments.count %>
  </li>
  <% end %>
</ul>
