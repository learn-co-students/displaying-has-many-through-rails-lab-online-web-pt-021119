<h1><%= @doctor.name %></h1>
<br><%= @doctor.department %>

 <ul>
	<% @doctor.appointments.each do |a| %>
		<li><%= link_to a.patient.name, patient_path(a.patient) %> - <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
	<% end %>
